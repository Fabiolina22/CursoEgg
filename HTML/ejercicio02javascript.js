let radio = prompt("ingrese el radio de una circunferencia");
let area = Math.PI * radio * radio;
let perimetro = 2 * Math.PI * radio;
var msg = `El area de la circunferencia es ${area}
y el perimetro es ${perimetro} `;
alert(msg);
